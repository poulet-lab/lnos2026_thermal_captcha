# Copy to .env and adjust for your setup
TCS_PORT=COM3
TCS_MAXIMUM_TEMPERATURE=45
MOCK_TCS=true
# 0 = leftmost monitor, 1 = second monitor, etc.
SECOND_MONITOR_INDEX=0
# Fallback x-offset if monitor detection fails (primary width in pixels)
SECOND_MONITOR_OFFSET=1920
